import type React from "react";
import { memo } from "react";
import Connected from "./Connected";

const SettingRelationshipRequests: React.FC = () => {
  return <Connected />;
};

export default memo(SettingRelationshipRequests);
