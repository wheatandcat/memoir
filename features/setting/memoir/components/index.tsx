import type React from "react";
import { memo } from "react";
import Connected from "./Connected";

const SettingMemoir: React.FC = () => {
  return <Connected />;
};

export default memo(SettingMemoir);
